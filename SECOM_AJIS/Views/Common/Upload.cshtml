 @{
    ViewBag.Title = "Upload";
    Layout = null;
}

@{
    var list = ViewBag.AttachFileList;
}

<script src="../../Scripts/jquery-1.5.1.min.js" type="text/javascript"></script>
<script type="text/javascript">
    function DeleteFile(id) {
        //$.ajax(
    }

    function findSize() {
        var fileInput = $("#loadfile")[0];
        alert(fileInput.files[0].fileSize + " bytes"); // Size returned in bytes.
    }

    

</script>
<table>
<tr><td>No.</td><td>File Name</td><td>Size</td><td>Action</td></tr>
@if (list != null) {
    int i=0; 
    foreach (var item in list) { 
        <tr><td>@(++i)</td><td>@item.FileName</td><td>@item.FileSize</td>
        <td><a href="/us/Common/DeleteFile/@item.AttachFileID" >Delete ID=@item.AttachFileID</a></td></tr>
    }
}

</table>

<a href="/us/Common/UpdateFlag">Update Flag</a>
<a href="/us/Common/CopyAttachFile">CopyAttachFile</a>
<a href="/us/Common/CancelUpload">CancelUpload</a>
@using (Html.BeginForm("TestUploadFile", "Common", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    
    <input type="file" name="uploadedFile" id="uploadedFile"  size="40" />
    <input type="submit" value="submit" />
    <input type="hidden" value="@Session.SessionID" name="TempRelatedID"/>
    
    <br/>
    <br/>
    <br/>
    <input type="file" id="loadfile" />
    <input type="button" value="find size" onclick="findSize()" />
    
    
}



